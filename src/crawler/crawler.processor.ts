// optional: if you want processor separated; processor is created via QueueService.createWorker in CrawlerService.onModuleInit

// kept intentionally minimal because processing function lives inside CrawlerService (processJob)

export {};
